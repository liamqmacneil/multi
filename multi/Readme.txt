obj_multi_inst - Any object that runs code that isn't client side should use this object as a parent

obj_multi_client - Gets server data and sends it to multi_inst objects

obj_multi_server - Starts the connection, defines const's,
Runs the server, sends out and receives data from clients